# План ближайших изменений

Работаем строго по одному пункту. Новый пункт начинаем только после проверки предыдущего в Roblox Studio. После успешной проверки отмечаем его как выполненный: `[x]`.

## Сейчас

- [x] **1. Исправить высоту установки башни**
  - Предпросмотр должен стоять ногами на поверхности под курсором.
  - После установки настоящая модель должна появляться на той же высоте.
  - Проверить несколько мест внутри разных `TowerZones`.

## Дальше

- [x] **2. Передать серверную работу с `TowerZones` в `MapService`**
  - `TowerService` больше не ищет `Workspace.MapData.TowerZones` самостоятельно.
  - `MapService` проверяет зоны при загрузке карты и определяет допустимую позицию.

- [x] **3. Закрыть прямую установку башни**
  - Сделать `TowerService.Place()` внутренней функцией.
  - Снаружи использовать только `TowerService.TryPlace()` с проверкой зоны, расстояния и денег.

- [x] **4. Исправить имя модели при репликации башни**
  - При обычной установке и позднем подключении отправлять одинаковый `ModelName`.

- [x] **5. Вынести минимальное расстояние между башнями в общий конфиг**
  - Клиент и сервер используют одно значение вместо двух отдельных чисел `4`.

- [x] **6. Начислять награду владельцу добившей башни**
  - За убийство получает деньги игрок, которому принадлежит башня.
  - Остальным игрокам награда за это убийство не начисляется.

- [x] **7. Исправить проверку запуска в `ServerMain`**
  - Проверить наличие `MapData` и успешный результат `MapService.Load()`.
  - Не запускать матч с неправильной картой.

- [x] **8. Добавить корректное завершение матча**
  - Поражение наступает при уничтожении базы.
  - Победа наступает после `WaveService.AllWavesCompleted`.
  - После завершения остановить игровые сервисы и симуляцию.

- [x] **9. Удалить пустой `EnemyTypes.luau`**
  - Перед удалением убедиться, что файл нигде не используется.

## Пока не делаем

- Не создаём новые `MatchService`, `DamageService`, `TargetingService` и placement-классы.
- Не разделяем существующие контроллеры на мелкие файлы.
- Не оптимизируем код без измеренной проблемы производительности.
- Не добавляем UI, upgrades, abilities и сложности в рамках этих пунктов.
